general {
    lock_cmd = pidof hyprlock || hyprlock       # dbus/sysd lock command
    before_sleep_cmd = loginctl lock-session    # lock before suspend
    after_sleep_cmd = hyprctl dispatch dpms on  # turn on display after sleep
}

listener {
    timeout = 600                                # 10 minutes
    on-timeout = loginctl lock-session           # lock screen
}

# THIS IS THE FIX
listener {
    timeout = 2                                  # A tiny timeout to trigger on wake
    on-resume = pkill -SIGUSR2 waybar            # Refreshes Waybar modules instantly
}
